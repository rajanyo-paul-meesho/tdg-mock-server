# Auto-generated schema file
# Generated by analyzing all configuration files across environments
# Keys marked as required=True are present in ALL deployable sections across ALL environments
# Note: Keys that have both direct values and sub-keys are shown in both nested and flattened formats

app:
  env: str(max=200, required=True)
  log:
    level: str(max=200, required=True)
  log-level: str(max=200, required=True)
  metric:
    sampling:
      rate: num(required=True)
  metric-sampling-rate: num(required=True)
  name: str(max=200, required=True)
  port: int(required=True)
catalog:
  filtering:
    grpc:
      channel:
        pool:
          size: int(required=True)
      connect:
        timeout:
          in:
            ms: int(required=True)
      connection:
        request:
          timeout:
            in:
              ms: int(required=True)
      deadline:
        in:
          sec: int(required=True)
      host: str(max=200, required=True)
      keep:
        alive:
          timeout:
            in:
              ms: int(required=True)
      plain:
        text: bool(required=True)
      port: int(required=True)
      queue:
        pool:
          size: int(required=True)
      thread:
        pool:
          size: int(required=True)
      timeout:
        in:
          ms: int(required=True)
  validity:
    redis:
      addr: str(max=200, required=True)
      db: int(required=True)
      min:
        idle: int(required=True)
      ping:
        flag: bool(required=True)
        interval:
          in:
            sec: int(required=True)
      pool:
        size: int(required=True)
        timeout:
          in:
            ms: int(required=True)
      read:
        timeout:
          in:
            ms: int(required=True)
      username: str(max=200, required=True)
      write:
        timeout:
          in:
            ms: int(required=True)
cross:
  sell:
    exploit:
      comp:
        sscat:
          mcache:
            id: int(required=True)
      feed:
        mcache:
          id: int(required=True)
      search:
        phrases:
          cache:
            enabled: bool(required=True)
            redis:
              addr: str(max=200, required=True)
              db: int(required=True)
              read:
                timeout:
                  in:
                    ms: int(required=True)
              write:
                timeout:
                  in:
                    ms: int(required=True)
cross-re:
  catalog-detail:
    in-memory-cache-enabled: bool(required=True)
  external-service:
    model-proxy:
      batch-size: int(required=True)
      host-details:
        rv-substitute-ad-scaleup:
          env-prefix: str(max=200, required=True)
      skip-mp-call: bool(required=True)
ct:
  budget:
    df:
      hosts: str(max=200, required=True)
      master: str(max=200, required=True)
    filter:
      dragonfly:
        master:
          name: str(max=200, required=True)
        read:
          from:
            replica: bool(required=True)
          timeout:
            in:
              ms: int(required=True)
        sentinel:
          nodes: str(max=200, required=True)
        write:
          timeout:
            in:
              ms: int(required=True)
deployable-name: str(max=200, required=True)
dynamic-config:
  optional: bool(required=True)
  source: str(max=200, required=True)
  version: int(required=True)
external-service:
  ab:
    auth: str(max=200, required=True)
    dial-timeout-in-ms: int(required=True)
    host: str(max=200, required=True)
    idle-conn-timeout-in-ms: int(required=True)
    max-idle-conns: int(required=True)
    max-idle-conns-per-host: int(required=True)
    port: int(required=True)
    read-timeout-in-ms: int(required=True)
fs:
  v4:
    grpc:
      plain:
        text: bool(required=True)
    host: str(max=200, required=True)
    port: int(required=True)
    timeout:
      in:
        ms: int(required=True)
in:
  memory:
    cache:
      size:
        in:
          bytes: int(required=True)
iop-starter:
  multi-re:
    cross-sell:
      default:
        exploit:
          logged-in:
            multi-experiment-data:
              cross_sell_ocp:
                config-filter-type: str(max=200, required=False)
                enabled: bool(required=False)
            variant-config:
              experiment:
                cross-sell-two-tower-exp:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
    rv-substitute:
      default:
        ad:
          anonymous:
            variant-config:
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
                new-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
          logged-in:
            multi-experiment-data:
              pdp_reco_ad_exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
            variant-config:
              experiment:
                rv_ad_control:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv_ad_cpc_discounting:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
              override:
                override-config:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                user-ids: str(max=200, required=True)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
                new-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
        exploit:
          anonymous:
            variant-config:
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
          logged-in:
            multi-experiment-data:
              recently_viewed_v2_substitute_experiment:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              rv-subs-ngst-exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
            variant-config:
              experiment:
                rv-exploit-exp-feed-length-50:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv-exploit-exp-feed-length-70:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv-exploit-experiment-merlin:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv-exploit-merlin-personalised-ranker:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv-exploit-mixed-feed-exp:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                rv-exploit-personalised-ranker:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                wishlist-exploit-subs-personalisation-ranker-exp:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                wishlist-reco-exploit-rv-ranker-exp:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
        explore:
          anonymous:
            variant-config:
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
          logged-in:
            multi-experiment-data:
              ct_holdout_exp_new:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              ct_qct_rv_slots:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              explore_holdout:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              explore_pdp_integration:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              ct_search_orchestrator_exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
            multi-user-segment-data:
              segment_skip_ct_qct:
                enabled: bool(required=True)
            variant-config:
              holdout:
                rv-holdout-empty-feed:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
              override:
                override-config:
                  config-bundle: json_str(required=True)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
              user-segment:
                segment_skip_ct_qct:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
      wishlist:
        ad:
          anonymous:
            variant-config:
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
                current-scaleup-percentage:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
          logged-in:
            multi-experiment-data:
              wishlist_reco_ad_exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              wishlist_reco_exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
            variant-config:
              experiment:
                wishlist_ad_subs_exp_relevance_0:
                  '6':
                    config-bundle: json_str(required=True)
                    logging-percent: int(required=True)
                  '7':
                    config-bundle: json_str(required=True)
                    logging-percent: int(required=True)
                  '8':
                    config-bundle: json_str(required=True)
                    logging-percent: int(required=True)
              override:
                override-config:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                user-ids: str(max=200, required=True)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
        exploit:
          anonymous:
            variant-config:
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
          logged-in:
            multi-experiment-data:
              recently_viewed_v2_substitute_experiment:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
              wishlist_reco_exp:
                config-filter-type: str(max=200, required=True)
                enabled: bool(required=True)
            variant-config:
              experiment:
                wishlist-exploit-subs-personalisation-ranker-exp:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                wishlist-exploit-substitute-experiment:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                wishlist-reco-exploit-rv-ranker-exp:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=True)
                  logging-percent: int(required=True)
                  scaleup-percent: int(required=True)
    rv-interactions:
      rv-interactions-v1:
        exploit:
          logged-in:
            variant-config:
              override:
                override-config:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                user-ids: str(max=200, required=False)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                  scaleup-percent: int(required=False)
      rv-interactions-v2:
        exploit:
          logged-in:
            variant-config:
              override:
                override-config:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                user-ids: str(max=200, required=False)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                  scaleup-percent: int(required=False)
      rv_interactions_intent:
        exploit:
          logged-in:
            variant-config:
              override:
                override-config:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                user-ids: str(max=200, required=False)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                  scaleup-percent: int(required=False)
      default:
        exploit:
          logged-in:
            variant-config:
              override:
                override-config:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                user-ids: str(max=200, required=False)
              scaleup:
                current-scaleup:
                  config-bundle: json_str(required=False)
                  logging-percent: int(required=False)
                  scaleup-percent: int(required=False)
is:
  v1:
    fallback:
      enabled: bool(required=True)
      grpc:
        plain:
          text: bool(required=True)
      host: str(max=200, required=True)
      port: int(required=True)
      timeout:
        in:
          ms: int(required=True)
    grpc:
      plain:
        text: bool(required=True)
    host: str(max=200, required=True)
    port: int(required=True)
    timeout:
      in:
        ms: int(required=True)
log:
  rb:
    draining:
      interval: int(required=True)
    size: int(required=True)
mcache:
  auth-token: str(max=200, required=True)
  catalog-validity:
    mcache-id: int(required=True)
mq:
  environment: str(max=200, required=False)
  server:
    api:
      url: str(max=200, required=False)
online:
  cg:
    c1:
      dial:
        timeout:
          in:
            ms: int(required=True)
      host: str(max=200, required=True)
      idle:
        conn:
          timeout:
            in:
              ms: int(required=True)
      max:
        idle:
          conns: int(required=True)
          conns_per_host: int(required=True)
      name: str(max=200, required=True)
      port: int(required=True)
      timeout:
        in:
          ms: int(required=True)
    c2:
      dial:
        timeout:
          in:
            ms: int(required=True)
      host: str(max=200, required=True)
      idle:
        conn:
          timeout:
            in:
              ms: int(required=True)
      max:
        idle:
          conns: int(required=True)
          conns_per_host: int(required=True)
      name: str(max=200, required=True)
      port: int(required=True)
      timeout:
        in:
          ms: int(required=True)
    c3:
      dial:
        timeout:
          in:
            ms: int(required=True)
      host: str(max=200, required=True)
      idle:
        conn:
          timeout:
            in:
              ms: int(required=True)
      max:
        idle:
          conns: int(required=True)
          conns_per_host: int(required=True)
      name: str(max=200, required=True)
      port: int(required=True)
      timeout:
        in:
          ms: int(required=True)
    cluster:
      count: int(required=True)
    grpc:
      c1:
        grpc:
          plain:
            text: bool(required=False)
        host: str(max=200, required=False)
        name: str(max=200, required=False)
        port: int(required=False)
        timeout:
          in:
            ms: int(required=False)
      c2:
        grpc:
          plain:
            text: bool(required=False)
        host: str(max=200, required=False)
        name: str(max=200, required=False)
        port: int(required=False)
        timeout:
          in:
            ms: int(required=False)
      c3:
        grpc:
          plain:
            text: bool(required=False)
        host: str(max=200, required=False)
        name: str(max=200, required=False)
        port: int(required=False)
        timeout:
          in:
            ms: int(required=False)
      cluster:
        count: int(required=False)
      enabled: bool(required=False)
orion:
  batch:
    size: int(required=True)
  caller:
    id: str(max=200, required=True)
    token: str(max=200, required=True)
  deadline: int(required=True)
  grpc:
    plain:
      text: bool(required=True)
  host: str(max=200, required=True)
  port: int(required=True)
prism:
  service:
    dial:
      timeout:
        in:
          ms: int(required=True)
    host: str(max=200, required=True)
    idle:
      conn:
        timeout:
          in:
            ms: int(required=True)
    max:
      idle:
        conns: int(required=True)
        conns_per_host: int(required=True)
    port: int(required=True)
    timeout:
      in:
        ms: int(required=True)
    username: str(max=200, required=True)
prismsdk:
  environment: str(max=200, required=False)
rv:
  substitute:
    ad:
      feed:
        redis:
          addr: str(max=200, required=True)
          db: int(required=True)
          ping:
            flag: bool(required=True)
            interval:
              in:
                sec: int(required=True)
          pool:
            size: int(required=True)
          read:
            timeout:
              in:
                ms: int(required=True)
          username: str(max=200, required=True)
          write:
            timeout:
              in:
                ms: int(required=True)
      scaleup:
        mp:
          grpc:
            lb:
              policy: str(max=200, required=True)
            plain:
              text: bool(required=True)
          host: str(max=200, required=True)
          port: int(required=True)
          timeout: int(required=True)
    exp:
      mp:
        grpc:
          lb:
            policy: str(max=200, required=True)
          plain:
            text: bool(required=True)
        host: str(max=200, required=True)
        port: int(required=True)
        timeout: int(required=True)
    exploit:
      feed:
        redis:
          addr: str(max=200, required=True)
          db: int(required=True)
          ping:
            flag: bool(required=True)
            interval:
              in:
                sec: int(required=True)
          pool:
            size: int(required=True)
          read:
            timeout:
              in:
                ms: int(required=True)
          username: str(max=200, required=True)
          write:
            timeout:
              in:
                ms: int(required=True)
      scaleup:
        host: str(max=200, required=True)
        mp:
          grpc:
            lb:
              policy: str(max=200, required=True)
            plain:
              text: bool(required=True)
          host: str(max=200, required=True)
          port: int(required=True)
          timeout: int(required=True)
    explore:
      feed:
        redis:
          addr: str(max=200, required=True)
          db: int(required=True)
          ping:
            flag: bool(required=True)
            interval:
              in:
                sec: int(required=True)
          pool:
            size: int(required=True)
          read:
            timeout:
              in:
                ms: int(required=True)
          username: str(max=200, required=True)
          write:
            timeout:
              in:
                ms: int(required=True)
      scaleup:
        mp:
          grpc:
            lb:
              policy: str(max=200, required=True)
            plain:
              text: bool(required=True)
          host: str(max=200, required=True)
          port: int(required=True)
          timeout: int(required=True)
rv-ad:
  external-service:
    model-proxy:
      batch-size: int(required=True)
      host-details:
        rv-substitute-ad-exp:
          env-prefix: str(max=200, required=True)
        rv-substitute-ad-experiment:
          env-prefix: str(max=200, required=True)
        rv-substitute-ad-scaleup:
          env-prefix: str(max=200, required=True)
      skip-mp-call: bool(required=True)
rv-exploit:
  external-service:
    model-proxy:
      batch-size: int(required=True)
      host-details:
        rv-substitute-exploit-experiment:
          env-prefix: str(max=200, required=True)
        rv-substitute-exploit-scaleup:
          env-prefix: str(max=200, required=True)
      skip-mp-call: bool(required=True)
rv-explore:
  external-service:
    model-proxy:
      batch-size: int(required=True)
      host-details:
        rv-substitute-exp:
          env-prefix: str(max=200, required=True)
        rv-substitute-explore-scaleup:
          env-prefix: str(max=200, required=True)
      skip-mp-call: bool(required=True)
segment:
  store:
    grpc:
      plain:
        text: bool(required=True)
    host: str(max=200, required=True)
    port: int(required=True)
    timeout: int(required=True)
static:
  slotting:
    grpc:
      plain:
        text: bool(required=True)
    host: str(max=200, required=True)
    port: int(required=True)
    timeout:
      in:
        ms: int(required=True)
zookeeper:
  periodic:
    update:
      enabled: bool(required=True)
      interval: str(max=200, required=True)
  server: str(max=200, required=True)
